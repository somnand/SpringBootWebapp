# SpringBootWebapp

src/main/java contains all Java classes. 
For mappings please refer the controllers or search "@RequestMapping("
```
WARNING : Please use jdk1.8 with update 66 or below because the JSPs don't compile with higher version.
```